namespace Cake.WebDeploy
{
    /// <summary>
    /// The type of agent used during the deplyment
    /// </summary>
    public enum RemoteAgent
    {
        WMSvc,
        MSDepSvc,
        TempAgent,
        None,
    }
}
